<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
"http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<section id="ediFormDetail" role="openmfg">
  <title>EDI Form Detail</title>
  <para>To create a new Form Detail query to be used when generating CSV file
  output, select the NEW button. The following screen will appear:</para>
  <para> <screenshot>
      <mediaobject>
        <imageobject>
          <imagedata fileref="images/ediFormDetail.png" />
        </imageobject>
        <caption>Create New EDI Form Detail Query</caption>
      </mediaobject>
    </screenshot></para>
  <para>When creating a new Form Detail query, you are presented with the
  following options:</para>
  <variablelist>
    <varlistentry>
      <term>Name</term>
      <listitem>
        <para>Enter a name to identify the Form Detail query. In the example
        shown, the name indicates the query will return information to be used
        in the header of the CSV file output.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>Order</term>
      <listitem>
        <para>Select a value between 1 and 99 using the arrow buttons located
        to the right of the field. The value entered specifies the order in
        which a query will be executed.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>Query</term>
      <listitem>
        <para>Enter a SQL query to identify the information you want to
        populate your CSV file with. As you can see in the example, the query
        selects invoice header information from the "invchead" table. You will
        note in the WHERE clause that MetaSQL is used to dynamically capture
        the specific Invoice number. For more information on writing queries
        using MetaSQL, please see the Report Writer User Guide.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>Notes</term>
      <listitem>
        <para>This is a scrolling text field with word-wrapping for entering
        Notes related to the EDI Detail query.</para>
      </listitem>
    </varlistentry>
  </variablelist>
  <para>To the far right of the screen, the following buttons are
  available:</para>
  <variablelist>
    <varlistentry>
      <term>CANCEL</term>
      <listitem>
        <para>Closes the screen without adding a new query to the Form Details
        list.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>OK</term>
      <listitem>
        <para>Creates the query and adds it to the master list of Form
        Details.</para>
      </listitem>
    </varlistentry>
  </variablelist>
</section>
